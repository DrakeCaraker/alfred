# Project

This is a fresh project. Run `/bootstrap` to generate your personalized CLAUDE.md.

## Quick Start

1. Run `/bootstrap` to set up your development environment
2. Run `/teach` to learn the first development pattern
3. Run `/status` to check your progress

This file will be replaced by `/bootstrap` with a persona-tailored version
including your project description, guardrails, and conventions.
